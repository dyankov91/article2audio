#!/usr/bin/env python3
"""a2pod-server — HTTP server for local podcast feed."""

import sys
from pathlib import Path

# Add lib to path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))

from server import run_server

if __name__ == "__main__":
    run_server()
